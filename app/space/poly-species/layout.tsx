import "@/styles/species-in-pieces.css";
import "@/styles/species-source-shapes.css";
import "@/styles/species-source-motion.css";

export default function PolySpeciesLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
